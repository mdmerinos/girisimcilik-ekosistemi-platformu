name: NATO DIANA Worker

on:
  workflow_dispatch:
  schedule:
    - cron: "20 6 * * *"
    - cron: "20 12 * * *"
    - cron: "20 18 * * *"

permissions:
  contents: read

concurrency:
  group: nato-diana-worker
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Verify worker secrets
        shell: bash
        env:
          WORKER_INGESTION_URL: ${{ secrets.WORKER_INGESTION_URL }}
          WORKER_INGESTION_SECRET: ${{ secrets.WORKER_INGESTION_SECRET }}
        run: |
          test -n "$WORKER_INGESTION_URL" || { echo "::error::WORKER_INGESTION_URL is not configured"; exit 1; }
          test -n "$WORKER_INGESTION_SECRET" || { echo "::error::WORKER_INGESTION_SECRET is not configured"; exit 1; }

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: workers/nato-diana/requirements.txt

      - name: Set up Chrome
        uses: browser-actions/setup-chrome@v1

      - name: Install worker dependencies
        run: python -m pip install -r workers/nato-diana/requirements.txt

      - name: Test NATO DIANA fallback parser
        run: python -m unittest workers/nato-diana/test_nato_diana_worker.py

      - name: Collect and submit NATO DIANA opportunities
        env:
          WORKER_INGESTION_URL: ${{ secrets.WORKER_INGESTION_URL }}
          WORKER_INGESTION_SECRET: ${{ secrets.WORKER_INGESTION_SECRET }}
        run: python workers/nato-diana/nato_diana_worker.py
